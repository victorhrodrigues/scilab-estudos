clear
clc
mode(0) // exibe os valores de variáveis, caso requerido
janela = winsid()
close(janela)

y0 = 1;
h = 0.25;
intervalo = [0:h:2]

function[y] = funcao1(x,y,intervalo)
    
function [k1, k2, k3, k4] = iteracoes(x, y, k)
    k1 = 
endfunction
